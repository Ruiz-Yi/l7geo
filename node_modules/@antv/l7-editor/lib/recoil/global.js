var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/recoil/global.ts
var global_exports = {};
__export(global_exports, {
  default: () => useGlobal
});
module.exports = __toCommonJS(global_exports);
var import_recoil = require("recoil");
var import_atomState = require("./atomState");
function useGlobal() {
  const [rightPanelWidth, setRightWidth] = (0, import_recoil.useRecoilState)(import_atomState.rightWidthState);
  const [mapOptions, setMapOptions] = (0, import_recoil.useRecoilState)(import_atomState.mapOptionState);
  const [layerColor, setLayerColor] = (0, import_recoil.useRecoilState)(import_atomState.layerColorState);
  const [hideEditor, setHideEditor] = (0, import_recoil.useRecoilState)(import_atomState.hideEditorState);
  const [autoFitBounds, setAutoFitBounds] = (0, import_recoil.useRecoilState)(import_atomState.autoFitBoundsState);
  const [popupTrigger, setPopupTrigger] = (0, import_recoil.useRecoilState)(import_atomState.popupTriggerState);
  const [baseMap, setBaseMap] = (0, import_recoil.useRecoilState)(import_atomState.baseMapState);
  const [activeTab, setActiveTab] = (0, import_recoil.useRecoilState)(import_atomState.activeTabState);
  const [layerType, setLayerType] = (0, import_recoil.useRecoilState)(import_atomState.officialLayersState);
  const [coordConvert, setCoordConvert] = (0, import_recoil.useRecoilState)(import_atomState.convertState);
  const [theme, setTheme] = (0, import_recoil.useRecoilState)(import_atomState.themeState);
  const [cityHistory, setCityHistory] = (0, import_recoil.useRecoilState)(import_atomState.cityHistoryState);
  const [showTextLayer, setShowTextLayer] = (0, import_recoil.useRecoilState)(import_atomState.showTextLayerState);
  const [textLayerFields, setTextLayerFields] = (0, import_recoil.useRecoilState)(import_atomState.textLayerFieldsState);
  const [customTiles, setCustomTiles] = (0, import_recoil.useRecoilState)(import_atomState.customTilesState);
  const [locale, setLocale] = (0, import_recoil.useRecoilState)(import_atomState.localeState);
  const [wasmPath, setWasmPath] = (0, import_recoil.useRecoilState)(import_atomState.wasmPathState);
  const [showDrawDistance, setShowDrawDistance] = (0, import_recoil.useRecoilState)(import_atomState.showDrawDistanceState);
  const [showDrawArea, setShowDrawArea] = (0, import_recoil.useRecoilState)(import_atomState.showDrawAreaState);
  return {
    rightPanelWidth,
    setRightWidth,
    mapOptions,
    setMapOptions,
    layerColor,
    setLayerColor,
    hideEditor,
    setHideEditor,
    popupTrigger,
    setPopupTrigger,
    baseMap,
    setBaseMap,
    autoFitBounds,
    setAutoFitBounds,
    activeTab,
    setActiveTab,
    layerType,
    setLayerType,
    coordConvert,
    setCoordConvert,
    theme,
    setTheme,
    cityHistory,
    setCityHistory,
    showTextLayer,
    setShowTextLayer,
    textLayerFields,
    setTextLayerFields,
    customTiles,
    setCustomTiles,
    locale,
    setLocale,
    wasmPath,
    setWasmPath,
    showDrawDistance,
    setShowDrawDistance,
    showDrawArea,
    setShowDrawArea
  };
}
