var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/components/app-header/btn/download-btn.tsx
var download_btn_exports = {};
__export(download_btn_exports, {
  default: () => download_btn_default
});
module.exports = __toCommonJS(download_btn_exports);
var import_icons = require("@ant-design/icons");
var import_turf = require("@turf/turf");
var import_antd = require("antd");
var import_react = __toESM(require("react"));
var import_react_i18next = require("react-i18next");
var import_tokml = __toESM(require("tokml"));
var import_utils = require("../../../utils");
var import_prettier_text = require("../../../utils/prettier-text");
var import_wkt = require("../../../utils/wkt");
var import_recoil = require("./../../../recoil");
var DownloadBtn = () => {
  const { editorText, fc } = (0, import_recoil.useFeature)();
  const { t } = (0, import_react_i18next.useTranslation)();
  const onDownload = (key) => {
    if (key === "GeoJson") {
      (0, import_utils.downloadText)(JSON.stringify(fc), "json");
    } else if (key === "FormatGeoJson") {
      const newText = (0, import_prettier_text.prettierText)({ content: fc });
      (0, import_utils.downloadText)(newText, "json");
    } else if (key === "LngLat") {
      (0, import_utils.downloadText)(
        (0, import_turf.coordAll)(fc).map((lngLat) => lngLat.join(",")).join(";"),
        "txt"
      );
    } else if (key === "KML") {
      const kml = (0, import_tokml.default)(fc);
      (0, import_utils.downloadText)(kml, "kml");
    } else if (key === "WKT") {
      (0, import_utils.downloadText)((0, import_wkt.GeoJSON2Wkt)(fc), "wkt");
    } else {
      (0, import_utils.downloadText)(editorText, "json");
    }
  };
  const DownloadMenuItems = [
    {
      key: "GeoJson",
      label: t("app_header.constants.xiaZaiGEO")
    },
    {
      key: "LngLat",
      label: t("app_header.constants.xiaZaiLNG")
    },
    {
      key: "KML",
      label: t("app_header.constants.xiaZaiKML")
    },
    {
      key: "WKT",
      label: t("app_header.constants.xiaZaiWKT")
    }
  ];
  return /* @__PURE__ */ import_react.default.createElement(
    import_antd.Dropdown,
    {
      menu: {
        items: DownloadMenuItems,
        onClick: ({ key }) => {
          onDownload(key);
        }
      }
    },
    /* @__PURE__ */ import_react.default.createElement(import_antd.Button, { id: "l7-editor-download", icon: /* @__PURE__ */ import_react.default.createElement(import_icons.CloudDownloadOutlined, null) }, t("btn.download_btn.xiaZai"))
  );
};
var download_btn_default = DownloadBtn;
