var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils/transform.ts
var transform_exports = {};
__export(transform_exports, {
  transformFeatures: () => transformFeatures
});
module.exports = __toCommonJS(transform_exports);
var import_turf = require("@turf/turf");
var import_constants = require("../constants");
var transformFeatures = (input, t) => {
  const json = JSON.parse(input);
  if (import_constants.FeatureCollectionVT.check(json)) {
    return json.features;
  }
  if (Array.isArray(json)) {
    if (import_constants.FeatureListVT.check(json)) {
      return json;
    }
    if (import_constants.GeometryListVT.check(json)) {
      return json.map((item) => (0, import_turf.feature)(item));
    }
    console.warn(t("utils.transform.weiShiBieJS"));
    return [];
  }
  if (import_constants.FeatureVT.check(json)) {
    return [json];
  }
  if (import_constants.GeometryVT.check(json)) {
    return [(0, import_turf.feature)(json)];
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  transformFeatures
});
