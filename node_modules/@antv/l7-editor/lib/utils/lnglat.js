var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils/lnglat.ts
var lnglat_exports = {};
__export(lnglat_exports, {
  GeoJSON2LngLat: () => GeoJSON2LngLat,
  LngLat2GeoJson: () => LngLat2GeoJson
});
module.exports = __toCommonJS(lnglat_exports);
var import_turf = require("@turf/turf");
var import_constants = require("../constants");
function GeoJSON2LngLat(fc) {
  const feature = (0, import_turf.coordAll)(fc).map((item) => item.join(",")).join(";");
  return feature;
}
function LngLat2GeoJson(lngLat) {
  const featurePositionList = lngLat.split("\n").filter((item) => item).map(
    (item) => item.split(";").filter((str) => str).map(
      (str) => str.split(",").filter((num) => num).map((num) => +num)
    )
  );
  if (import_constants.LngLatVT.check(featurePositionList)) {
    const newFeatures = [];
    newFeatures.push(
      ...featurePositionList.flat().map((position) => (0, import_turf.point)(position))
    );
    return newFeatures;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  GeoJSON2LngLat,
  LngLat2GeoJson
});
