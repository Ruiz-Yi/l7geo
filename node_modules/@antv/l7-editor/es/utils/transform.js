import { feature } from '@turf/turf';
import { FeatureCollectionVT, FeatureListVT, FeatureVT, GeometryListVT, GeometryVT } from "../constants";
export var transformFeatures = function transformFeatures(input, t) {
  var json = JSON.parse(input);
  if (FeatureCollectionVT.check(json)) {
    return json.features;
  }
  if (Array.isArray(json)) {
    if (FeatureListVT.check(json)) {
      return json;
    }
    if (GeometryListVT.check(json)) {
      return json.map(function (item) {
        return feature(item);
      });
    }
    console.warn(t('utils.transform.weiShiBieJS'));
    return [];
  }
  if (FeatureVT.check(json)) {
    return [json];
  }
  if (GeometryVT.check(json)) {
    return [feature(json)];
  }
};