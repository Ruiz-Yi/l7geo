import { length, lineSliceAlong, point } from '@turf/turf';
import { last } from 'lodash-es';
export var centerOfLine = function centerOfLine(line) {
  var lineLength = length(line, {
    units: 'meters'
  });
  var position = last(lineSliceAlong(line, 0, lineLength / 2, {
    units: 'meters'
  }).geometry.coordinates);
  return point(position);
};