import gcoord from 'gcoord';
export var wgs84togcj02 = function wgs84togcj02(feature) {
  if (feature.geometry.type.match(/polygon/i)) {
    feature.geometry = JSON.parse(JSON.stringify(feature.geometry));
  }
  // @ts-ignore
  return gcoord.transform(feature, gcoord.WGS84, gcoord.GCJ02);
};
export var gcj02towgs84 = function gcj02towgs84(feature) {
  if (feature.geometry.type.match(/polygon/i)) {
    feature.geometry = JSON.parse(JSON.stringify(feature.geometry));
  }
  // @ts-ignore
  return gcoord.transform(feature, gcoord.GCJ02, gcoord.WGS84);
};