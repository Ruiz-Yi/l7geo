import prettier from 'prettier';
import parserBabel from 'prettier/parser-babel';
export function prettierText(options) {
  var content = options.content,
    _options$parser = options.parser,
    parser = _options$parser === void 0 ? 'json' : _options$parser;
  var newContent = content;
  if (typeof content !== 'string') {
    newContent = JSON.stringify(content, null, 2);
  }
  var newText = prettier.format(newContent, {
    parser: parser === 'json' ? parser : 'babel',
    plugins: [parserBabel]
  });
  return newText;
}