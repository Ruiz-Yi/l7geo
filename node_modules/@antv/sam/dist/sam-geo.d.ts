import { SAM } from './sam';
import { MapHelper } from './utils/geo';
export interface IGeoImageOption {
    extent: [number, number, number, number] | undefined;
    width: number;
    height: number;
}
export declare class SAMGeo extends SAM {
    private imageOption;
    private imageBounds;
    private metersPerpixelsX;
    private metersPerpixelsY;
    mapHelper: MapHelper;
    setGeoImage(image: HTMLImageElement | string, imageOption: IGeoImageOption): Promise<void>;
    exportGeoPolygon(output: any, simplifyThreshold?: number): Promise<{
        type: string;
        features: {
            type: string;
            properties: {};
            geometry: {
                type: string;
                coordinates: [number, number][][];
            };
        }[];
    }>;
    lngLat2ImagePixel(lnglat: [number, number]): number[] | undefined;
}
